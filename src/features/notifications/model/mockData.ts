/**
 * Notifications Mock Data
 * 
 * Re-export from centralized mocks for backward compatibility.
 * @see @/shared/data/mocks/notifications.mock.ts
 */

export { 
  mockNotifications, 
  getNotificationStats, 
  getUnreadNotifications,
  filterNotifications,
} from '@/shared/data/mocks';

// Note: Types are exported from './types', not from here to avoid duplicates

